#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(a==b)
    {
        printf ("a=b ");
    }
    else if (a<b)
    {
        printf ("a is small");
    }
    else if (a>b)
    {
        printf ("b is small");
    }
    else{
        printf ("none of the above"); 
    }

   // printf ("b=%d",b);
    return 0;
}
