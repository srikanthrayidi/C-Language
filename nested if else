#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(a>=b)
    {
        if(a==b)
        {
            printf("a  is equal to b in if blockf");
        }
        else
        {
             printf("a greater than b but not equal to b");
        }
    }
    else //if (a<=b)
    {
        if(a==b)
        {
            printf("a equal to b in else if block ");
        }
        else
        {
             printf("a less than b but not equal to b");
        }
    }

    return 0;
}
